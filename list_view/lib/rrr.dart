import 'package:flutter/material.dart';

// ignore: camel_case_types
class rama extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200.0,
      child: Center(child: Text('rama shaker')),
      color: Colors.blue ,

    );
  }
}
